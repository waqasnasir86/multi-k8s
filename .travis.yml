language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t waqas86/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true waqas86/react-test npm test

after_success:
  - docker build -t waqas86/multi-client ./client
  - docker build -t waqas86/multi-nginx ./nginx
  - docker build -t waqas86/multi-server ./server
  - docker build -t waqas86/multi-worker ./worker

  # login to the docker CLI using encrypted variables
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # takes these images and push them to docker hub
  - docker push waqas86/multi-client
  - docker push waqas86/multi-nginx
  - docker push waqas86/multi-server
  - docker push waqas86/multi-worker